



// eslint-disable-next-line react/prop-types
const Watches = ({watch}) => {

    return (
        <div>
            <h1>Watches: {watch.length} </h1>
        </div>
    );
};

export default Watches;